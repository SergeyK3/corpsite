{
  "name": "corpsite",
  "private": true,
  "scripts": {
    "db": "docker compose up -d --wait",
    "backend": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "frontend": "npm run dev --prefix corpsite-ui",
    "dev": "npm run db && concurrently -k -n backend,frontend -c blue,green \"npm run backend\" \"wait-on http-get://127.0.0.1:8000/health -t 60000 && npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.3"
  }
}
